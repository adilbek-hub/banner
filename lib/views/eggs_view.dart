import 'package:flutter/material.dart';

class EggsView extends StatelessWidget {
  const EggsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('EggsView'),
      ),
    );
  }
}
